# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GoogleIndoorMapping' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
   pod 'MapsIndoors'
   pod 'GPUImage', '~> 0.1.7'
   pod 'GoogleMaps'
  pod 'GooglePlaces'

  # Pods for GoogleIndoorMapping

  target 'GoogleIndoorMappingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GoogleIndoorMappingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
